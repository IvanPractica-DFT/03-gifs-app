#GifsApp

Este es un proyecto de Angular.

## DEV
1. Clonar el repositorio
2. Instalar las dependencias con `npm install`
3. Iniciar el servdor con ne `ng serve`
4. Navegar a `http://localhost:4200/`
